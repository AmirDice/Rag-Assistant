{
  "description": "Perceptual hashes (pHash) of known logos to filter out during image extraction. Empty by default — add an entry per logo you want skipped (name, phash, reference_file). threshold=14 catches scale/compression variants without cross-logo false positives.",
  "threshold": 14,
  "logos": []
}
